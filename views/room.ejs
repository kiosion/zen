<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zen</title>
    <script>
        const roomName = "<%= roomName %>"
    </script>
    <script defer src="http://localhost:3000/socket.io/socket.io.js"></script>
    <script defer src="script.js"></script>
    <script type="text/javascript">
        function FocusOnInput()
        {
             document.getElementById("message-input").focus();
        }
    </script>
    <style>
        html {
            background-color: black;
            color: white;
            font-family:'Courier New', Courier, monospace;
        }

        html::-webkit-scrollbar {
            display: none;
        }

        html {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        body {
            padding: 0;
            margin: 0;
            display: flex;
            justify-content: center;
        }

        p {
            padding-left: 2%;
            padding-right: 2%;
            font-family: 'Courier New', Courier, monospace;
            text-decoration: none;
            color: white;
            word-wrap: word-break;
        }

        mark {
            font-family: 'Courier New', Courier, monospace;
            font-weight: bold;
            background-color: transparent;
            color: white;
        }

        a {
            cursor: pointer;
            color: white;
            text-decoration: none;
            transition: all 0.2s ease-in-out;
        }

        a:visited {
            color: white;
            text-decoration: none;
        }

        a:hover {
            text-shadow: 0 0 10px white;
        }

        #header {
            background: black;
            position: fixed;
            padding-top: 4vh;
            top: 0;
            border-bottom: 1px solid #1a1a1a;
            max-width: 1000px;
            width: 80%;
            display: flex;
        }

        div#topl {
            width: 100%;
            max-width: 500px;
            display: flex;
            align-content: flex-start;
            justify-content: left;
        }
        div#topr {
            width: 100%;
            max-width: 500px;
            display: flex;
            align-content: flex-end;
            justify-content: right;
        }

        #container {
            position: absolute;
            top:0; bottom: 0;
            margin: auto;
            height: 90vh;
            width: 90vw;
            max-width: 1000px;
        }

        #message-container {
            padding-left: 4vw;
            padding-right: 4vw;
            padding-top: 4vh;
            width: 100%;
            max-width: 1000px;
        }

        #message-container div {
            width: 80%;
            border-bottom: 1px solid #1a1a1a;
            padding: 2px;
        }

        #message-container div:nth-child(2n) {
            background-color: black;
        }

        .send-container {
            background-color: black;
            position: fixed;
            padding-bottom: 4vh;
            bottom: 0;
            border-top: 1px solid #1a1a1a;
            max-width: 1000px;
            width: 80%;
            display: flex;
        }

        .msgform-container {
            padding-left: 4vw;
            padding-right: 4vw;
            padding-top: 2vh;
            width: 100%;
        }

        .msgform-container input {
            width: 80%;
            background: transparent;
            color: white;
            border: none;
            border-bottom: 1px solid #1a1a1a;
            height: 120%;
            font-family: 'Courier New', Courier, monospace;
            font-size: 12pt;
        }

        input:focus,select:focus,textarea:focus,button:focus {
            outline: none;
        }

        .msgform-container button {
            color: white;
            cursor: pointer;
            background: transparent;
            border: none;
            margin: 1px;
            height: 120%;
            font-family: 'Courier New', Courier, monospace;
            text-transform: uppercase;
            transition: all .2s ease-in-out;
            font-size: 12pt;
        }

        .msgform-container button:hover {
            text-shadow: 0 0 10px white;
        }

        #message-input {
            flex-grow: 1;
        }
    </style>
</head>

<body onload="FocusOnInput()"">
    <div id="container">
        <div id="header">
            <div id="topl"><p><mark><a href="/"><span>< Back to index</span></a></mark></p></div>
            <div id="topr"><p><mark><a onclick="navigator.clipboard.writeText(window.location.href)"><span>> Copy room link</span></a></mark></p></div>
        </div>
        <div id="message-container"></div>
        <div class="send-container">
            <form id="usrnameform" class="msgform-container">
                <input type="text" id="usrname-input" placeholder="Enter a username...">
                <button type="submit" id="send-button">> Done</button>
            </form>
            <form id="msgform" class="msgform-container">
                <input type="text" id="message-input" placeholder="Message...">
                <button type="submit" id="send-button">> Send</button>
            </form>
        </div>
    </div>
</body>

</html>
